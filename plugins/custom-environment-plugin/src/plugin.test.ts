import { customEnvironmentPluginPlugin } from './plugin';

describe('custom-environment-plugin', () => {
  it('should export plugin', () => {
    expect(customEnvironmentPluginPlugin).toBeDefined();
  });
});
